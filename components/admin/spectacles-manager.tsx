"use client";
import Link from "next/link";
import { type FormEvent, useState } from "react";
import { Actions, Area, Drawer, ErrorMessage, Field, FormSection, Select, localDate, ImageField } from "./form-ui";

type Item = { _id: string; title: string; slug: string; status: "draft" | "published"; season: string; audienceLabel: string; description: string; applicationDeadline: string; image: string; creationLabel: string; director: string; partnerCount: string; facts: { label: string; value: string }[]; plateaux: { time: string; title: string; description: string }[]; provides: string[]; venueProvides: string[]; tourDates: { window: string; zone: string; status: string }[]; technique: { label: string; value: string }[]; slots: string[]; firstParts: string[]; faq: { question: string; answer: string }[] };
const empty = { title: "", slug: "", status: "draft", season: "", audienceLabel: "Offre aux professionnels", description: "", applicationDeadline: "", image: "", creationLabel: "CRÉATION", director: "", partnerCount: "", facts: "Durée | \nPlateau | \nJauge conseillée | \nPublic | \nLangue | ", plateaux: "", provides: "", venueProvides: "", tourDates: "", technique: "", slots: "À définir", firstParts: "Compagnie locale\nAtelier amateur\nÉcole / conservatoire\nCréation maison\nPas encore décidé", faq: "" } as const;
export function SpectaclesManager({ initialItems }: { initialItems: Item[] }) { const [items, setItems] = useState(initialItems); const [editing, setEditing] = useState<Item | "new" | null>(null); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(""); const current = editing === "new" ? null : editing; const formData = new FormData(event.currentTarget); const response = await fetch(current ? `/api/admin/spectacles/${current._id}` : "/api/admin/spectacles", { method: current ? "PUT" : "POST", body: formData }); const result = await response.json(); setSaving(false); if (!response.ok) return setError(result.message || "Enregistrement impossible."); setItems(list => current ? list.map(x => x._id === current._id ? result.spectacle : x) : [result.spectacle, ...list]); setEditing(null); } async function remove(item: Item) { if (!confirm(`Supprimer définitivement « ${item.title} » ?`)) return; const response = await fetch(`/api/admin/spectacles/${item._id}`, { method: "DELETE" }); if (!response.ok) return setError("Suppression impossible."); setItems(list => list.filter(x => x._id !== item._id)); } return <><div className="mb-9 flex flex-wrap items-end justify-between gap-5"><div><p className="mb-2 text-[10px] uppercase tracking-[.24em] text-[#EF2F29]">Diffusion professionnelle</p><h1 className="font-display text-[clamp(38px,5vw,64px)]">Spectacles</h1><p className="text-sm text-[#77716B]">{items.length} spectacle{items.length !== 1 ? "s" : ""}</p></div><button onClick={() => setEditing("new")} className="min-h-12 bg-[#EF2F29] px-6 text-[11px] uppercase tracking-[.17em] text-white">+ Créer un spectacle</button></div>{error && !editing ? <ErrorMessage>{error}</ErrorMessage> : null}<div className="overflow-x-auto border border-black/[.09] bg-[#FBFAF7]"><table className="w-full min-w-[700px] text-left"><thead><tr className="border-b border-black/10 text-[10px] uppercase tracking-[.18em] text-[#837D77]"><th className="px-6 py-4">Spectacle</th><th className="px-4 py-4">Saison</th><th className="px-4 py-4">Statut</th><th className="px-6 py-4 text-right">Actions</th></tr></thead><tbody>{items.map(item => <tr key={item._id} className="border-b border-black/[.06] even:bg-black/[.02] hover:bg-black/[.04] transition-colors"><td className="px-6 py-5"><p className="font-display text-xl">{item.title}</p><small>/{item.slug}</small></td><td className="px-4 py-5 text-sm">{item.season}</td><td className="px-4 py-5 text-sm">{item.status === "published" ? "Publié" : "Brouillon"}</td><td className="px-6 py-5"><div className="flex justify-end gap-2">{item.status === "published" ? <Link href={`/spectacles/${item.slug}`} target="_blank" className="flex items-center gap-1.5 bg-[#EF2F29] text-white px-3 py-1.5 text-[10px] uppercase tracking-[.1em] font-medium transition hover:bg-black"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="h-3 w-3"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg> Voir</Link> : null}<button onClick={() => setEditing(item)} className="flex items-center gap-1.5 border border-black/15 bg-white px-3 py-1.5 text-[10px] uppercase tracking-[.1em] font-medium transition hover:border-[#EF2F29] hover:text-[#EF2F29]"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="h-3 w-3"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg> Modifier</button><button onClick={() => remove(item)} className="flex items-center gap-1.5 px-3 py-1.5 text-[10px] uppercase tracking-[.1em] font-medium text-red-600 transition hover:text-red-800"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="h-3 w-3"><path d="M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg> Supprimer</button></div></td></tr>)}{!items.length ? <tr><td colSpan={4} className="py-20 text-center font-display text-2xl text-[#625D58]">Aucun spectacle</td></tr> : null}</tbody></table></div>{editing ? <SpectacleForm item={editing === "new" ? null : editing} saving={saving} error={error} close={() => setEditing(null)} save={save} /> : null}</>; }
function SpectacleForm({ item, saving, error, close, save }: { item: Item | null; saving: boolean; error: string; close: () => void; save: (e: FormEvent<HTMLFormElement>) => void }) { const v = item ? { ...item, applicationDeadline: localDate(item.applicationDeadline), facts: item.facts.map(x => `${x.label} | ${x.value}`).join("\n"), plateaux: item.plateaux.map(x => `${x.time} | ${x.title} | ${x.description}`).join("\n"), provides: item.provides.join("\n"), venueProvides: item.venueProvides.join("\n"), tourDates: item.tourDates.map(x => `${x.window} | ${x.zone} | ${x.status}`).join("\n"), technique: item.technique.map(x => `${x.label} | ${x.value}`).join("\n"), slots: item.slots.join("\n"), firstParts: item.firstParts.join("\n"), faq: item.faq.map(x => `${x.question} | ${x.answer}`).join("\n") } : empty; return <Drawer title={item ? "Modifier le spectacle" : "Nouveau spectacle"} onClose={close}><form onSubmit={save} className="space-y-10 p-[clamp(22px,4vw,44px)]">{error ? <ErrorMessage>{error}</ErrorMessage> : null}<FormSection title="Identité professionnelle"><div className="grid gap-5 sm:grid-cols-2"><Field label="Titre *" name="title" defaultValue={v.title} required /><Select label="Statut" name="status" defaultValue={v.status} options={["draft|Brouillon", "published|Publié"]} /><Field label="Saison *" name="season" defaultValue={v.season} required /><Field label="Public cible" name="audienceLabel" defaultValue={v.audienceLabel} /><Field label="Clôture candidatures *" name="applicationDeadline" type="datetime-local" defaultValue={v.applicationDeadline} required /><Field label="Mise en scène" name="director" defaultValue={v.director} /><Field label="Création" name="creationLabel" defaultValue={v.creationLabel} /><Field label="Lieux partenaires" name="partnerCount" defaultValue={v.partnerCount} /></div><Area label="Présentation professionnelle *" name="description" defaultValue={v.description} required /><ImageField label="Affiche" name="imageFile" defaultValue={v.image} hint="Ratio recommandé: 3:4" ratio="3/4" /></FormSection><FormSection title="Dispositif et tournée"><Area label="Informations clés" hint="libellé | valeur" name="facts" defaultValue={v.facts} rows={6} /><Area label="Déroulé des plateaux" hint="heure | titre | description" name="plateaux" defaultValue={v.plateaux} rows={5} /><Area label="Ce que nous apportons" hint="une ligne par élément" name="provides" defaultValue={v.provides} /><Area label="Ce que le lieu apporte" hint="une ligne par élément" name="venueProvides" defaultValue={v.venueProvides} /><Area label="Fenêtres de tournée" hint="période | zone | disponibilité" name="tourDates" defaultValue={v.tourDates} /><Area label="Fiche technique" hint="libellé | valeur" name="technique" defaultValue={v.technique} /></FormSection><FormSection title="Candidature"><Area label="Créneaux proposés" name="slots" defaultValue={v.slots} /><Area label="Types de première partie" name="firstParts" defaultValue={v.firstParts} /><Area label="Questions fréquentes" hint="question | réponse" name="faq" defaultValue={v.faq} rows={5} /></FormSection><Actions saving={saving} onClose={close} /></form></Drawer>; }
